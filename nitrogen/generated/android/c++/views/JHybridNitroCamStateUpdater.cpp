///
/// JHybridNitroCamStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridNitroCamStateUpdater.hpp"
#include "views/HybridNitroCamComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::nitrocam::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroCamState>;

void JHybridNitroCamStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroCamSpec::javaobject> javaView,
                                           jni::alias_ref<react::StateWrapperImpl::javaobject> stateWrapper) {
  JHybridNitroCamSpec* view = javaView->cthis();
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroCamState& data = concreteState->getData();
  const std::optional<HybridNitroCamProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridNitroCamState's data doesn't contain any props!");
  }
  const HybridNitroCamProps& props = maybeProps.value();
  if (props.isRed.isDirty) {
    view->setIsRed(props.isRed.value);
    // TODO: Set isDirty = false
  }
  if (props.isFrontCamera.isDirty) {
    view->setIsFrontCamera(props.isFrontCamera.value);
    // TODO: Set isDirty = false
  }
  if (props.flash.isDirty) {
    view->setFlash(props.flash.value);
    // TODO: Set isDirty = false
  }
  if (props.zoom.isDirty) {
    view->setZoom(props.zoom.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridNitroCamSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nitrocam::views
